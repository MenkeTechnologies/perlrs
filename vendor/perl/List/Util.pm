# Stub only: `List::Util` is implemented natively in perlrs (`src/list_util.rs`) so every
# EXPORT_OK name matches Perl 5 without loading XS. This file exists for `%INC` / tooling.

package List::Util;

our $VERSION = '1.68';

1;
